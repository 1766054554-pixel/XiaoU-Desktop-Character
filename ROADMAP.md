# 路线图

## 双角色一起互动

下一阶段将支持情侣双方各自定制一个角色，并让两个角色同时出现在一台电脑的桌面上。两个角色必须保持各自的形象、动作、状态和对白归属，不能通过复制或换色冒充第二个角色。

计划能力：

- 分别加载两套角色清单与本地定制配置，素材互不混用；
- 两个角色可以独立移动，同时共享屏幕边界、距离和碰撞信息；
- 靠近、碰面或用户主动选择后触发同步动作，例如打招呼、击掌、分享食物、一起工作、合照和散步；
- 定制项目可以在双方同意后增加牵手、拥抱、发送啵啵等互动；
- 双方对白按角色分别显示，支持一问一答和动作完成后的回应；
- 使用一个中性的“表情与互动”入口选择单人或双人动作；
- 原始照片、配对配置和定制对白默认只保存在本地；
- 两个角色及共同动作都经过视觉确认后，才构建 Windows 或 macOS 安装包。

实现顺序：

1. 将单角色运行状态抽象为可复用实例，同时保持当前行为不变；
2. 加入双角色距离检测、动作锁和同步时间线；
3. 制作独立的双人动作清单、对白协议与人工验收流程；
4. 完成 Windows 与 macOS 的双实例压力测试、隐私审计和打包。

首个公开版本仍保持单角色，先验证跨平台稳定性、动作质量、隐私边界和发布流程。双角色功能在真正完成同步动作与双平台测试后再发布，不把路线图写成现有能力。
